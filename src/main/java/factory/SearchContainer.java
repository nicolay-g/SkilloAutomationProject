package factory;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;

public class SearchContainer {
    /*
    public dynamic myRecord;
    public List<ResultRecord> resultRecords;

    private List<WebElement> resultRecordsElements;
    private void setRecords(){
        for(WebElement item : resultRecordsElements) {
            int buttonsCount = item.findElements(By.cssSelector("button.btn")).size();
            if (buttonsCount == 0)) {
                myRecord = new BaseResultRecord(obj, item);
            } else {
                ResultRecord resultRecord = new ResultRecord(...);
                resultRecords.add(resultRecord);
            }
            //item.
        }
    }
    */
}
